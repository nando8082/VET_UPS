import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Veterinaria_UPS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
